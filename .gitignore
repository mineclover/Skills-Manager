# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/*
.agents/
.ar-home/
.playwright-cli/
.worktrees/
.private-features/
.superpowers/
docs/
temp.md

# Design assets (local only)
design/

# Local tool config
config/

# Landing page worktree (lives on the private-only `landing` orphan branch)
landing/

# SEO research docs (private, lives on landing branch)
research/

# Bundled CLI binary (staged by scripts/bundle-cli.mjs at build time)
src-tauri/resources/

# Cargo build output. Since the workspace root moved here, cargo writes to
# ./target for every member (crates/core, crates/cli, src-tauri).
/target/
